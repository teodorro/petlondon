import { Box } from '@mui/material';
import React from 'react';

export default function MapPage() {
  return (
    <Box
      // className="absolute bottom-0 left-0 right-0 top-0"
      className="absolute bottom-0 left-0 right-0 top-0"
      // sx={{ border: '3px solid #f00' }}
    >
      MapPage
    </Box>
  );
}
